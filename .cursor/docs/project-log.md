# JONBEATZ.DEV — Project Log

## 2026-06-25 — Project created
- Split the red `jonbeatz.dev` variant out of the JonBeatz personal profile into this
  standalone project (Tier 3). Curated scaffold: site + UI/3D skills + docs + scripts.
- Red baked as the default variant. Mem0 scope `jonbeatz_dev`. See `ReCall.md` for detail.

## 2026-06-25 — v1.0.0 to GitHub + workflow/voice/MCP parity
- GitHub repo `jonbeatz/JonBeatz-Dev` created, v1.0.0 release, README screenshots; repo
  flipped **public**.
- Added OmniVoice (ritual-only), project MCPs (gitignored config + example + `sync:mcp-env`),
  and full workflow rituals (workflow/interactive-workflows/voice-policy rules + Update-Docs
  prompt + npm scripts). Boundary updated: voice + MCPs in-scope; Telegram/DeepSeek/ComfyUI/
  Google/image-gen remain personal.
